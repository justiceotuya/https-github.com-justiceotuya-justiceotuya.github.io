import * as React from 'react';

import {
    Layout,
    IntroSection,
    ShowCaseSection,
    ContactSection
} from '../component';

const IndexPage = () => (
    <Layout>
        <IntroSection />
        <ShowCaseSection />
        <ContactSection />
    </Layout>
);

export default IndexPage;
